apiVersion: v2
name: processed-data
description: A deployment providing processed data collected during beamline session as part of the graph federation
type: application

version: 0.1.0

dependencies:
  - name: processed-data
    version: 0.1.0
    condition: processed-data.enabled
